apiVersion: v1
kind: Service
metadata:
  name: {{ include "linter.fullname" . }}
  labels:
    {{- include "linter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "linter.selectorLabels" . | nindent 4 }}
